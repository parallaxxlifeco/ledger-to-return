# Generated by build/make-test-pages.py — rebuild with one command, never commit
build/local.html
build/local-db.html

# Generated by test/screenshots.mjs
screenshots/

node_modules/
.DS_Store

# Never commit anything holding transaction data or credentials
*backup*.json
ledger-to-return-data.json

# Real bank statements. THIS REPOSITORY IS PUBLIC — never commit one.
# test/fixtures/README.md says how to put one back locally.
test/fixtures/*.pdf
test/fixtures/*.csv
# except this one: invented names and ids, real shape. The Wise test needs it.
!test/fixtures/wise-sample.csv

# symlink made by build/make-test-pages.py so build/ can reach ../vendor
build/vendor
